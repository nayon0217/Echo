# ECHO verification pipeline (Python side). Pinned loosely; tighten before demo.

psycopg[binary]>=3.2,<4      # Postgres driver (v3), binary build = no local libpq needed
python-dotenv>=1.0,<2        # load DATABASE_URL and friends from .env

# --- Ingestion (policy.md §3, §13) ---
trafilatura>=1.12,<3         # HTML -> clean text/markdown extraction
PyYAML>=6.0,<7               # parse ingest/sources.yaml

# --- Pipeline HTTP + LLM (policy.md §1–§2, §4–§7, §13) ---
anthropic>=0.70              # Claude client; structured output / tool-use
pydantic>=2.7,<3             # schema-enforced LLM output (policy.md §2)
fastapi>=0.115,<1            # app/webhook.py
uvicorn[standard]>=0.30,<1   # ASGI server for the above
python-multipart>=0.0.9      # file uploads to /transcribe and /extract

# --- Speech to text (policy.md §1 stage 1, §2) ---
faster-whisper>=1.1,<2       # CTranslate2 Whisper; exposes per-segment logprobs

# --- Text to speech (specs.md §2: reply by voice, not text) ---
edge-tts>=7,<8               # neural TTS, no API key; MP3 -> OGG/Opus via PyAV
