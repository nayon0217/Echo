{
  "name": "echo-whatsapp-bot",
  "version": "0.1.0",
  "description": "ECHO — Voice-first AI verification WhatsApp bot for migrant workers (Step 1: language selection)",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
